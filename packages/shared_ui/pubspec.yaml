name: shared_ui
version: 0.1.0
publish_to: none
description: Shared Flutter UI building blocks for PDO Lite Next.

environment:
  sdk: ^3.10.8
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
